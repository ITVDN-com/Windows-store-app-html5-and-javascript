(function () {
    "use strict";

    // WinJS.UI.Pages.define - ассоциирует относительный путь с объектом контролирующим выполнение страницы.
    WinJS.UI.Pages.define("/pages/home/home.html", {

        // Данная функция вызывается каждый раз когда пользоватль переходит на эту страницу.
        ready: function (element, options) {
        }
    });
})();
